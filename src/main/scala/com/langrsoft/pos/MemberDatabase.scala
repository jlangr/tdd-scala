package com.langrsoft.pos

class MemberDatabase {
  def memberLookup(phoneNumber: String): Member = {
    throw new RuntimeException("The member retrival system is currently unavailable. Please check back in 6-8 weeks.")
  }
}
